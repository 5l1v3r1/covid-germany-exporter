apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-covid-exporter.fullname" . }}
  labels:
    {{- include "prometheus-covid-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus-covid-exporter.selectorLabels" . | nindent 4 }}
